# tuples
# Tuples are immutable
# Tuples are oriented collection of items
# Tuples can store any data type
# We can't change (add or delete) vcalues from tuple once it created
# but we can add, delete data from list which is present inside tuples

mixed = ('one',2,3.0,4,5)
# on append , on pop , on insert , on remove { this method are not used inside tuples }
# only count and index { This methods are used inside tuples }

# functions
# min() , max() , len() , sum()

mixed2 = (1,2,3,4,5,[6,7,8])
mixed2[5].pop()

print(mixed2)